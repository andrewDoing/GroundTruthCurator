- [x] add curation instructions doc per dataset
- [x] expose that via endpoint

- [x] make bucket a guid, handle the application of guids in the API

- separate tags from built-in and extensible
- auto-apply train and validation splits and judge-training tags automatically
- sme shouldn't add tags for split
- snapshot controls the split?

- make topic tags user definable, but also reusable from existing