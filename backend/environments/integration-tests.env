# Integration test defaults for Ground Truth Curator (backend)
#
# This file is safe to commit. It provides non-secret overrides specific to
# running integration tests, layered on top of environments/sample.env.

# Integration tests use per-test databases/containers and should not re-seed
# tags or reinitialize repos during app lifespan.
GTC_COSMOS_TEST_MODE=true

# Keep search disabled unless explicitly configured in local.env or CI.

# Avoid Easy Auth in integration tests unless explicitly enabled.
GTC_EZAUTH_ENABLED=false
