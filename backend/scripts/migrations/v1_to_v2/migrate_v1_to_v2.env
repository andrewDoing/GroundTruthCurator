# Sample configuration for migrate_v1_to_v2.py script
# Copy this file to migrate_v1_to_v2.env and update with your values

# Cosmos DB Configuration
# For production: Use your Azure Cosmos DB endpoint URL
# For local: Use https://localhost:8081
GTC_COSMOS_ENDPOINT=http://localhost:8081

# Database name (default: gt-curator)
GTC_COSMOS_DB_NAME=gt-curator

# Container name for ground truth items (default: ground_truth)
GTC_COSMOS_CONTAINER_GT=ground_truth

# Use Cosmos DB Emulator (set to 'true' for local development)
# When true, uses the well-known emulator key and disables SSL verification
# When false, uses DefaultAzureCredential for authentication
GTC_USE_COSMOS_EMULATOR=true
